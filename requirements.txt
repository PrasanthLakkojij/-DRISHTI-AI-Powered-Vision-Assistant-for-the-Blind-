# Verified working on Python 3.9.12 (Windows, CPU-only PyTorch build)
# Install with: pip install -r requirements.txt

numpy==1.26.4
sounddevice==0.4.6
pygame==2.5.2
edge-tts==7.2.8
scipy==1.13.0
opencv-contrib-python==4.10.0.84
pillow==11.3.0

# PyTorch CPU build — this exact pair is required on Python 3.9
# (PyTorch 2.9+ dropped Python 3.9 support entirely)
torch==2.1.0
torchvision==0.16.0

transformers==4.40.1
speechbrain==1.0.0

# face_recognition needs cmake + dlib built first — see README if install fails
cmake==4.3.2
dlib==19.22.1
face-recognition==1.3.0
face_recognition_models==0.3.0

ultralytics==8.2.18
ultralytics-thop==2.0.19

openwakeword==0.6.0
PyAudio==0.2.14
SpeechRecognition==3.10.1

twilio==9.0.5
groq==1.0.0
python-dotenv==1.2.1
